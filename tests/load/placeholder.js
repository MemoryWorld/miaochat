console.log(
  "Load test scenarios live under tests/load/*.js. Run them with the k6 CLI, e.g. `k6 run tests/load/session-list.js`. See tests/load/README.md for environment variables and thresholds."
);
