# Database

This directory contains the Release 1 PostgreSQL schema, generated migrations, and seed scripts.

The schema intentionally includes future-ready tenancy fields such as `workspace_id` so Release 1 does not block later workspace and permission work.
