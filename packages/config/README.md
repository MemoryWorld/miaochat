# @agenthub/config

Shared lint, TypeScript, and Vitest defaults for the AgentHub workspace.

This package is intentionally lightweight so the rest of the monorepo can adopt one set of baseline tool settings without duplicating local config files.
