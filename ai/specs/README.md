# Specs

The currently approved product spec is the repository root file [SPEC.md](../../SPEC.md).

This directory is reserved for versioned or snapshot specs when the active specification changes over time.
